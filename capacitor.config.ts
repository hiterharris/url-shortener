import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.urlshortener.hiterharris',
  appName: 'URL Shortener',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
